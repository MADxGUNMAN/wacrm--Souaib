# clasp's real project file. Generated by `cp .clasp.json.example
# .clasp.json` (or `clasp clone`). Untracked because a fork or a second
# environment points at a different script, and because clasp rewrites
# `rootDir` to an absolute local path on some platforms.
.clasp.json

# clasp's OAuth credentials for YOUR Google account. Never commit.
.clasprc.json

node_modules/
